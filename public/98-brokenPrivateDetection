
Fs_Customized_Bash_broken_private()
{
	local ret=$?

	# private/99-placeholder sets this variable. Check that file for details
	if [ -n "${FS_CUSTOMIZED_BASH_SUBMITTED_FILES_IN_PRIVATE}" ]; then
		if "${FS_CUSTOMIZED_BASH_SUBMITTED_FILES_IN_PRIVATE}"; then
			echo "FS_CUSTOMIZED_BASH CONTENTS ARE SUBMITTED IN PRIVATE "
		fi
	fi
	return $ret
}
